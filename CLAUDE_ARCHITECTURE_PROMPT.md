# 🎯 پرامپت طراحی معماری نهایی — برای Claude

> **روش استفاده:** کل این فایل را کپی کنید و به Claude بدهید. حتماً در همان جلسه‌ای که به پوشه پروژه دسترسی دارد (یا فایل‌های PROJECT_ANALYSIS.md و PROJECT_GRAPH.md را هم ضمیمه کنید) استفاده کنید.

---

کپی از اینجا به پایین ⬇️

---

## نقش تو

تو یک **معمار ارشد نرم‌افزار (Principal Software Architect)** با ۱۵+ سال تجربه در طراحی سامانه‌های دولتی و خدمات شهروندی در مقیاس ملی، متخصص معماری‌های وب Progressive Web App، امنیت اطلاعات، و سیستم‌های Real-Time هستی.

## ماموریت تو

یک **سند معماری نهایی، دقیق و قابل پیاده‌سازی (Implementation-Ready Architecture Document)** برای بازنویسی حرفه‌ای پروژه «سامانه جامع خدمات شهروندی و پیشخوان هوشمند» تولید کن — به‌گونه‌ای که بتوان آن سند را **عیناً به مدل‌های هوش مصنوعی دیگر یا تیم توسعه داد و بدون هیچ ابهامی پیاده‌سازی شود.**

## گام صفر — مطالعه اجباری پیش از هر چیز

قبل از هر خروجی یا سؤالی، این فایل‌ها را در پوشه پروژه کامل بخوان و تحلیل کن:

1. `PROJECT_ANALYSIS.md` — تحلیل کامل پروژه فعلی (استک، بخش‌ها، نقاط قوت/ضعف)
2. `PROJECT_GRAPH.md` — گراف معماری، God Nodes، کامونیتی‌های ۱۳گانه و چرخه عمر پرونده
3. `graphify-out/GRAPH_REPORT.md` — گزارش خودکار گراف دانش
4. `src/types.ts` — مدل داده کامل فعلی (اینترفیس‌های CitizenService، PishkhanOffice، CaseRequest، CaseTimelineStep، ConsultationAdvisor و...)
5. `src/App.tsx` و پوشه `src/components/` — منطق و فلوهای واقعی (دو حالت شهروند/اپراتور، ۹ مودال، ۵ تب)
6. `src/data/mockData.ts` و `src/data/consultationData.ts` — دامنه کسب‌وکار (۱۰+ دسته خدمت، ۱۰ کد علت نقص مدرک، گردش کار ۸ مرحله‌ای پرونده)

از این فایل‌ها **دامنه کسب‌وکار، موجودیت‌ها و فلوها را استخراج کن** — این‌ها Requirements واقعی محصول هستند و سند معماری تو باید ۱۰۰٪ آن‌ها را پوشش دهد، نه کمتر.

## قیود قطعی و غیرقابل مذاکره (Hard Constraints)

1. **ممنوعیت کامل:** هیچ Native App (Android/iOS)، Flutter، React Native، Kotlin، Swift — «هیچ‌جا در پروژه» استفاده نشود.
2. **شهروندان:** باید روی **هر گوشی و هر مرورگری** (اندروید پایین، iPhone قدیمی، مرورگرهای ضعیف) بدون نصب چیزی استفاده کنند → راه‌حل باید **PWA یا Web App ریسپانسیو موبایل-اول** باشد با پشتیبانی Offline.
3. **اپراتورهای دفتر پیشخوان:** روی **سیستم دسکتاپ** (مانیتور بزرگ، ماوس و کیبورد) کار می‌کنند → پنل اپراتور باید Desktop-First Web باشد. (یک کدبیس، دو تجربه: موبایل-اول برای شهروند، دسکتاپ-اول برای دفتر — یا دو اپ جدا در مونوریپو؛ تو تصمیم بگیر و دلیل بیاور.)
4. **زبان و جهت:** محصول ۱۰۰٪ فارسی RTL است اما معماری باید i18n-Ready باشد.
5. **هوش مصنوعی:** چت‌بات و دستیار صوتی که در پروتوتایپ Rule-based بودند، باید با **Gemini API** (کلید از قبل موجود) به‌صورت واقعی و سمت سرور پیاده شوند — نه سمت کلاینت.
6. **چیز فعلی فقط پروتوتایپ Mock است:** هیچ بک‌اند، دیتابیس، احراز هویت و ذخیره‌سازی ندارد. تو باید کل Stack سمت سرور و زیرساخت را از صفر طراحی کنی.

## قوانین تعامل — «همیشه بپرس»

این بخش مهم‌ترین قانون کاری توست:

- **هیچ فرض مهمی بدون پرسیدن نکن.** قبل از نهایی کردن هر لایه معماری، سؤالات دقیق و گزینه‌دار (هر سؤال با ۲–۴ گزینه + پیشنهاد خودت + دلیل) از من بپرس.
- کار را **فازبندی‌شده** جلو ببر: اول سؤالات دامنه کسب‌وکار → بعد سؤالات فنی → بعد پیش‌نویس معماری → بعد سؤالات ریز → بعد سند نهایی.
- در هر فاز، اگر بین گزینه‌ها تعادل (Trade-off) وجود دارد، جدول مقایسه با معیارهای هزینه، سرعت توسعه، مقیاس‌پذیری و نگه‌داری ارائه بده و بعد توصیه قطعی بده.
- اگر جواب من «تو تصمیم بگیر» بود، بهترین گزینه را انتخاب کن و در سند نهایی بخش «تصمیمات گرفته‌شده به‌جای کارفرما» با دلیل ثبت کن.

سؤالاتی که حتماً باید بپرسی (لیست حداقلی — تو کاملش کن):

1. مقیاس هدف: چند هزار یا چند میلیون کاربر؟ چند دفتر پیشخوان؟ چند شهر؟
2. بودجه زیرساخت و ترجیح هاست (ابر خارجی، داخلی، Hybrid — با توجه به داده دولتی شهروندان)؟
3. احراز هویت: اتصال به سامانه‌های واقعی دولت (ثبت احوال، شاهکار، پیامک OTP) الان ممکن است یا Mock بماند؟
4. پرداخت: درگاه بانکی کدام؟ (زرین‌پال، آی‌دی‌پی، ...)؟
5. تیم پیاده‌سازی چیست؟ (فقط مدل‌های AI؟ توسعه‌دهنده انسانی؟ چند نفر؟) — این بر پیچیدگی معماری اثر مستقیم دارد.
6. مهلت نسخه اول (MVP) و تعریف MVP از نظر تو کارفرما چیست؟

## سند خروجی نهایی — ساختار الزامی

سند نهایی را با این فصل‌ها و این سطح از جزئیات بده (هر بخش باید آن‌قدر دقیق باشد که مدل دیگر بدون سؤال از تو بتواند کدش را بنویسد):

1. **خلاصه اجرایی** — یک صفحه: معماری انتخابی، استک نهایی، دلیل انتخاب در ۱۰ خط.
2. **ماتریس تصمیم فنی** — جدول هر لایه (Frontend، Backend، DB، Cache، Queue، Storage، Real-Time، AI، Map، Payment، SMS، Auth، Hosting، CI/CD، Monitoring) با: گزینه‌های بررسی‌شده، انتخاب نهایی، دلیل، جایگزین قابل قبول.
3. **معماری سیستم (C4 Model)** — دیاگرام‌های Mermaid: Context، Container، Component + دیاگرام جریان درخواست خدمت از ثبت تا تحویل.
4. **معماری Frontend** — معماری پوشه‌بندی دقیق (Monorepo؟)، استراتژی State Management، Data Fetching/Caching، Routing، طراحی PWA (Service Worker، Offline Strategy، Update Flow)، Design System (بر پایه همان زبان بصری Liquid Glass پروتوتایپ)، استراتژی RTL/i18n، بودجه عملکرد (Performance Budget با اعداد دقیق).
5. **معماری Backend** — انتخاب معماری (Modular Monolith یا Microservice — با جدول تصمیم)، ساختار پوشه ماژول‌ها، الگوی Domain-Driven Design روی موجودیت‌های types.ts، طراحی REST/GraphQL کامل با نمونه قرارداد (Request/Response JSON) برای ۱۰ Endpoint کلیدی، استراتژی Real-Time (تغییر وضعیت پرونده، صف دفتر، پیام‌رسان)، سیستم صف کارهای پس‌زمینه (استعلام‌های دولتی، اعلان‌ها).
6. **مدل داده و دیتابیس** — ERD کامل Mermaid روی پایه types.ts (نرمال‌سازی، ایندکس‌ها، Migration، Redis برای چه چیزی، Object Storage برای مدارک با رمزنگاری).
7. **امنیت (فصل جدی)** — احراز هویت دو مرحله‌ای OTP، RBAC دقیق (شهروند / اپراتور دفتر / مدیر دفتر / ادمین سامانه)، رمزنگاری مدارک (At-Rest / In-Transit)، حفاظت PII شهروندان، Rate Limiting، Audit Log کامل برای اقدامات اپراتورها، چک‌لیست OWASP Top 10.
8. **یکپارچه‌سازی‌های خارجی** — Gemini (چت‌بات و تحلیل مدرک)، درگاه پرداخت، پیامک، سامانه‌های دولتی با الگوی Anti-Corruption Layer و Adapter برای هرکدام.
9. **قابلیت اطمینان و مقیاس** — استراتژی Deployment، Scaling، Backup، Disaster Recovery، SLOها با عدد، Monitoring و Alerting.
10. **کیفیت** — استراتژی تست (Unit/Integration/E2E با ابزار دقیق)، Code Style، CI/CD Pipeline گام‌به‌گام.
11. **نقشه راه پیاده‌سازی** — تقسیم کل کار به فازهای مشخص (هر فاز: ماژول‌ها، وابستگی‌ها، معیار پذیرش/Acceptance Criteria قابل تست، تخمین)، طوری که هر فاز بتواند مستقیم به یک مدل AI دیگر داده شود.
12. **پروتکل تحویل به مدل‌های پیاده‌ساز** — قالب استاندارد Task File برای هر فاز (زمینه، فایل‌های مرتبط، محدودیت‌ها، تعریف انجام‌شده) که فقط کافیست کپی شود به مدل بعدی.

## قواعد کیفیت خروجی

- هر تصمیم معماری باید «چرا» داشته باشد — نه فقط «چه».
- از پروتوتایپ فعلی فقط **زبان بصری، دامنه کسب‌وکار و فلوها** را بگیر؛ مشکلاتش (فایل‌های ۴۰۰۰ خطی، Mock بودن، وابستگی‌های مرده @google/genai و express) را تکرار نکن و برای هرکدام راه‌حل معماری بده.
- همه دیاگرام‌ها با Mermaid باشند (قابل رندر در Markdown).
- هیچ Placeholder یا «بعداً تصمیم می‌گیریم» در سند نهایی نباشد.
- سند نهایی را در فایل `ARCHITECTURE.md` در ریشه پروژه ذخیره کن.

## شروع کن

حالا با **گام صفر (خواندن فایل‌ها)** شروع کن، خلاصه فهم خودت را در ۱۵ خط گزارش بده، و بعد **اولین دسته سؤالات دامنه کسب‌وکار** را بپرس. سند نهایی را فقط بعد از جواب‌های من و تأیید پیش‌نویس تولید کن.

